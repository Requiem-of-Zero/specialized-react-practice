import React from "react";
import { BannerWrapper } from "./Banner.styles";

const Banner = ({subtitle, title, ctaOne, imgUrl}) => {
  return <BannerWrapper imgUrl={imgUrl}>
    
  </BannerWrapper>;
};

export default Banner